---
name: self-registration
type: glossary
description: A unit adding itself to a shared registry at load or construction time, so a central coordinator never hand-lists every unit.
---

A unit adding itself to a shared registry at load or construction time, so
a central coordinator never hand-lists every unit.

See patterns: [[self-registering-unit]], [[registry-and-compose]],
[[boring-composition-over-dynamic-loading]].
